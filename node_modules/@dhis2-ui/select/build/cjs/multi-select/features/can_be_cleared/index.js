"use strict";

require("../common");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a clearable MultiSelect with a selection and onchange handler is rendered', () => {
  cy.visitStory('MultiSelect', 'With clear button, selection and onChange');
});
(0, _steps.When)('the clear button is clicked', () => {
  cy.get('[data-test="dhis2-uicore-multiselect-clear"]').click();
});
(0, _steps.Then)('the MultiSelect is cleared', () => {
  cy.window().should(win => {
    expect(win.onChange).to.be.calledOnce;
    expect(win.onChange).to.be.calledWith({
      selected: []
    });
  });
});