"use strict";

require("../common");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a MultiSelect with a prefix and no selection is rendered', () => {
  cy.visitStory('MultiSelect', 'With prefix');
});
(0, _steps.Given)('a MultiSelect with a prefix and a selection is rendered', () => {
  cy.visitStory('MultiSelect', 'With prefix and selection');
});
(0, _steps.Then)('the prefix is shown', () => {
  cy.get('[data-test="dhis2-uicore-multiselect-prefix"]').contains('Prefix text').should('be.visible');
});
(0, _steps.Then)('the selection is shown', () => {
  cy.contains('option one').should('be.visible');
});