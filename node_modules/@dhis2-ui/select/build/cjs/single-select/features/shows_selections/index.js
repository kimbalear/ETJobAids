"use strict";

require("../common");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a SingleSelect with options and a selection is rendered', () => {
  cy.visitStory('SingleSelect', 'With options and a selection');
});
(0, _steps.Then)('the selection is displayed', () => {
  cy.contains('option one').should('be.visible');
});