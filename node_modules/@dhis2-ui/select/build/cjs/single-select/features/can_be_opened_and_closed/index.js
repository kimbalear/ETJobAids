"use strict";

require("../common");

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a SingleSelect with options is rendered', () => {
  cy.visitStory('SingleSelect', 'With options');
});
(0, _steps.Given)('the SingleSelect is closed', () => {
  cy.contains('option one').should('not.exist');
  cy.contains('option two').should('not.exist');
  cy.contains('option three').should('not.exist');
});
(0, _steps.Given)('the SingleSelect is focused', () => {
  cy.get('[data-test="dhis2-uicore-select-input"]').focus();
  cy.focused().should('have.attr', 'data-test', 'dhis2-uicore-select-input');
});
(0, _steps.When)('the down arrowkey is pressed on the focused element', () => {
  cy.focused().type('{downarrow}');
});
(0, _steps.When)('the spacebar is pressed on the focused element', () => {
  cy.focused().type(' ');
});
(0, _steps.When)('the up arrowkey is pressed on the focused element', () => {
  cy.focused().type('{uparrow}');
});
(0, _steps.When)('the escape key is pressed on the focused element', () => {
  cy.focused().type('{esc}');
});