"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a loading FileListItem with onCancel handler is rendered', () => {
  cy.visitStory('FileListItem', 'Loading with onCancel');
});
(0, _steps.When)('the user clicks on the cancel text', () => {
  cy.get('[data-test="dhis2-uicore-filelistitem-cancel"]').click();
});
(0, _steps.Then)('the onCancel handler is called', () => {
  cy.window().should(win => {
    expect(win.onCancel).to.be.calledWith({});
  });
});