"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a FileListItem with Onremove handler is rendered', () => {
  cy.visitStory('FileListItem', 'With onRemove');
});
(0, _steps.When)('the user clicks on the remove text', () => {
  cy.get('[data-test="dhis2-uicore-filelistitem-remove"]').click();
});
(0, _steps.Then)('the onRemove handler is called', () => {
  cy.window().should(win => {
    expect(win.onRemove).to.be.calledWith({});
  });
});