"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a default FileInputFieldWithList is rendered', () => {
  cy.visitStory('FileInputFieldWithList', 'With default texts');
});
(0, _steps.Then)('the default button label is visible', () => {
  cy.contains('Upload a file').should('be.visible');
});