"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a MenuItem with onClick handler and value is rendered', () => {
  cy.visitStory('MenuItem', 'With On Click And Value');
});
(0, _steps.When)('the MenuItem is clicked', () => {
  cy.get('[data-test="dhis2-uicore-menuitem"]').click();
});
(0, _steps.Then)('the onClick handler is called with value', () => {
  cy.window().should(win => {
    expect(win.onClick).to.be.calledWith({
      value: 'Value'
    });
  });
});