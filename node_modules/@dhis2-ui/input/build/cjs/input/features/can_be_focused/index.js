"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a Input with onFocus handler is rendered', () => {
  cy.visitStory('Input', 'With onFocus');
});
(0, _steps.When)('the Input is focused', () => {
  cy.get('[data-test="dhis2-uicore-input"] input').focus();
});
(0, _steps.Then)('the onFocus handler is called', () => {
  cy.window().should(win => {
    expect(win.onFocus).to.be.calledWith({
      value: '',
      name: 'Default'
    });
  });
});