"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a Input with onChange handler is rendered', () => {
  cy.visitStory('Input', 'With onChange');
});
(0, _steps.When)('the Input is filled with a character', () => {
  cy.get('[data-test="dhis2-uicore-input"]').click().type('a');
});
(0, _steps.Then)('the onChange handler is called', () => {
  cy.window().should(win => {
    expect(win.onChange).to.be.calledWith({
      value: 'a',
      name: 'Default'
    });
  });
});