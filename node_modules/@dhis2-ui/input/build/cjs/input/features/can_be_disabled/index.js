"use strict";

var _steps = require("cypress-cucumber-preprocessor/steps");

(0, _steps.Given)('a disabled Input is rendered', () => {
  cy.visitStory('Input', 'With disabled');
});
(0, _steps.When)('the user clicks the input', () => {
  cy.get('[data-test="dhis2-uicore-input"] input').click({
    force: true
  });
});
(0, _steps.Then)('the Input is not focused', () => {
  cy.focused().should('not.exist');
});